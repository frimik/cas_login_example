== CAS Client login app with your own login form

This is an example app that demonstrates how to use the RubyCAS-client gem to authenticate your users without having the users redirected to the CAS servers web page.  The user never leaves your app and uses your app's login form to authenticate.  

